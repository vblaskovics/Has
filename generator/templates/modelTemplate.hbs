module.exports = (sequelize, Sequelize) => {
  const {{seed.className}} = sequelize.define('{{seed.modelName}}', {
  {{#each seed.fields}}
    {{this.name}}: {
      type: Sequelize.{{this.type}},
    },
  {{/each}}
  })

  return {{seed.className}}
}
